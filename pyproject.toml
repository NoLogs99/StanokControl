[project]
name = "stanok-control"
version = "0.1.0"
description = "Will"
requires-python = ">="
authors = [{name = "NoLogs99"}]

dependencies = [
    "pathlib >=",
    "dotenv >=",
    "python-dotenv >=",
    "loguru >=",
]
